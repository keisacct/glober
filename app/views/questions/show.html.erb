<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @question.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @question.content %>
</p>

<%= link_to 'Edit', edit_question_path(@question) %> 
<%= link_to 'Destroy', question_path, method: :delete, data: { confirm: '本当に質問を削除しますか？' } %>
<%= link_to 'Back', questions_path %>
