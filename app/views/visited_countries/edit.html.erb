<%= render 'shared/second_header' %>

<%= form_with model: @visited_country, local: true do |f| %>
    <%= f.label :tag_list %>
    <%= text_field_tag 'visited_country[tag_list]', @visited_country.tag_list.join(",") %>
    <%= f.submit "完了" %>
<% end %>